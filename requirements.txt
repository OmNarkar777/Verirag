fastapi==0.111.0
uvicorn==0.29.0
python-multipart==0.0.9

sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
# psycopg[asyncio,binary] is used on Vercel instead of asyncpg.
# - asyncio: enables async SQLAlchemy support via asyncio.start_tls()
#   (Python ssl module, bypasses uvloop libuv SSL which returns EBUSY)
# - binary: bundles libpq inside the wheel — no system libpq needed on
#   Vercel Lambda (Amazon Linux 2 / manylinux2014_x86_64 compatible)
psycopg[asyncio,binary]==3.2.3
alembic==1.13.1

pydantic==2.11.7
pydantic-settings==2.7.1
python-dotenv==1.0.1

langchain==0.2.1
langchain-groq==0.1.6
langchain-community==0.2.1
langchain-core==0.2.3
langsmith==0.1.147

huggingface_hub==0.23.2

# chromadb is intentionally excluded from production (Vercel).
# Reasons: (1) requires libgomp.so.1 (not on Lambda), (2) onnxruntime
# transitive dep is ~200 MB pushing bundle over Vercel's 500 MB limit.
# All vectorstore/pipeline code gracefully degrades when chromadb is absent.
# For local full-RAG development: pip install chromadb==0.5.0

ragas==0.1.9
datasets==2.19.1

loguru==0.7.2

pypdf==4.2.0
tiktoken==0.7.0

httpx==0.27.0
numpy==1.26.4
